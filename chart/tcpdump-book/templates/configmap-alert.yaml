{{- if gt (len .Values.alerting_groups) 0 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "book.fullname" . }}-alerting-rules
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ template "book.name" . }}
    chart: {{ template "book.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
  {{ template "book.fullname" . }}-alerting-rules.yaml: |-
    groups:
    {{- toYaml .Values.alerting_groups | nindent 6 }}
{{- end }}