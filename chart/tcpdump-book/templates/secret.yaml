apiVersion: v1
kind: Secret
metadata:
  name: {{ template "book.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ template "book.name" . }}
    chart: {{ template "book.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
  nginx.conf: {{ tpl (toYaml .Values.config) . | b64enc}}
